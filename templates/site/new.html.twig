{% extends 'base.html.twig' %}

{% block title %}New Site{% endblock %}

{% block body %}
<div class="container">
    <div>
        <a class="btn btn-primary" href="{{ path('site_index') }}">La Configuration</a>
    </div>
    <br/>

    {{ include('site/_form.html.twig') }}
</div>
{% endblock %}
