{% extends 'base.html.twig' %}

{% block title %}Vente{% endblock %}

{% block body %}
    <h1>Vente</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Quantite</th>
                <td>{{ vente.quantite }}</td>
            </tr>
            <tr>
                <th>Date d'achat</th>
                <td>{{ vente.venteAt ? vente.venteAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('vente_index') }}">Liste des ventes</a>

    {# <a href="{{ path('vente_edit', {'id': vente.id}) }}">edit</a> #}

    {# {{ include('vente/_delete_form.html.twig') }} #}
{% endblock %}
